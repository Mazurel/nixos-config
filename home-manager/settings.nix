# This file contains settings for the whole configuration
# It does not use options so removing variables will
# fail whole rebuilding process
{
  dwm-enable = true;
  nixos = {
    
  };
  home-manager = {

  };
}
